version: "3.8"
services:
    # Mysql database container
    db:
        image: mysql
        container_name: pocs7_db
        restart: always
        volumes:
            - db-data:/var/lib/mysql
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
        networks:
            - pocs7

    # phpmyadmin container
    phpmyadmin:
        image: phpmyadmin
        container_name: pocs7_phpmyadmin
        restart: always
        depends_on:
            - db
        ports:
            - 8085:80
        environment:
            PMA_HOST: db
        networks:
            - pocs7

    # Symfony apache project container
    www:
        build: docker/php
        container_name: pocs7_symfony
        ports:
          - "8741:80"
        volumes:
            - ./docker/php/vhosts:/etc/apache2/sites-enabled
            - ./:/var/www
        restart: always
        networks:
            - pocs7

networks:
    pocs7:

volumes:
    db-data: